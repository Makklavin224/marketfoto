---
phase: 10-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/index.html
  - frontend/src/pages/LandingPage.tsx
autonomous: true
requirements: [LAND-08, LAND-10]

must_haves:
  truths:
    - "HTML <head> contains SEO title, description, and OG tags for MarketFoto"
    - "LCP is under 3 seconds with no render-blocking resources from landing sections"
    - "Before/After placeholder images and below-fold sections use lazy loading"
  artifacts:
    - path: "frontend/index.html"
      provides: "SEO meta tags, OG tags, canonical URL"
      contains: "MarketFoto"
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Lazy-loaded below-fold sections for performance"
  key_links:
    - from: "frontend/index.html"
      to: "Search engines"
      via: "meta tags (title, description, og:*)"
      pattern: "og:title|og:description|og:image"
    - from: "frontend/src/pages/LandingPage.tsx"
      to: "frontend/src/components/landing/*.tsx"
      via: "React.lazy for below-fold sections"
      pattern: "React\\.lazy|Suspense"
---

<objective>
Add SEO meta tags (title, description, OG tags) and optimize landing page performance for LCP < 3 seconds through lazy loading of below-fold sections and image optimization hints.

Purpose: Search engine discoverability and fast first-paint are critical for a conversion-focused landing page. Meta tags drive click-through from search results, and fast LCP reduces bounce rate.

Output: Updated index.html with SEO tags + updated LandingPage.tsx with lazy loading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-01-SUMMARY.md

<interfaces>
From frontend/index.html (existing):
```html
<!-- Standard Vite index.html with <div id="root"></div> -->
<!-- Add meta tags to <head> -->
```

From frontend/src/pages/LandingPage.tsx (created in Plan 01):
```typescript
// Currently imports all 7 sections eagerly
// Optimize: lazy-load below-fold sections (everything after HeroSection)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO meta tags and performance optimization</name>
  <files>frontend/index.html, frontend/src/pages/LandingPage.tsx</files>
  <action>
Read first:
- frontend/index.html (current <head> content)
- frontend/src/pages/LandingPage.tsx (current imports and structure)

**Update frontend/index.html <head> section (per D-08, LAND-08):**

Add these meta tags inside `<head>`:

```html
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<!-- SEO -->
<title>MarketFoto — карточки для WB и Ozon за 30 секунд</title>
<meta name="description" content="Создавайте профессиональные карточки товаров для Wildberries, Ozon и Яндекс.Маркет за 30 секунд. AI удаляет фон, вы выбираете шаблон — готово. 3 карточки бесплатно." />
<meta name="keywords" content="карточки для маркетплейсов, инфографика WB, карточки Ozon, удаление фона, MarketFoto" />
<link rel="canonical" href="https://marketfoto.ru/" />

<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:title" content="MarketFoto — карточки для WB и Ozon за 30 секунд" />
<meta property="og:description" content="AI убирает фон, вы выбираете шаблон с инфографикой — готовая карточка для маркетплейса за 30 секунд. Попробуйте бесплатно." />
<meta property="og:url" content="https://marketfoto.ru/" />
<meta property="og:site_name" content="MarketFoto" />
<meta property="og:locale" content="ru_RU" />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="MarketFoto — карточки для WB и Ozon за 30 секунд" />
<meta name="twitter:description" content="AI убирает фон, вы выбираете шаблон — готовая карточка для маркетплейса за 30 секунд." />

<!-- Performance hints -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="dns-prefetch" href="https://fonts.googleapis.com" />
```

Keep existing content (the Vite script tag, <div id="root">, etc.). Only ADD meta tags to <head>, do not remove anything.

Note: og:image is omitted for now because we don't have a social preview image yet. Add a TODO comment: `<!-- TODO: Add og:image when social preview image is created -->`.

**Update frontend/src/pages/LandingPage.tsx (per D-10, LAND-10):**

Optimize for LCP by lazy-loading below-fold sections. The HeroSection must load eagerly (it IS the LCP content). Everything below the fold can load lazily.

Replace static imports of below-fold sections with React.lazy:

```typescript
import HeroSection from "../components/landing/HeroSection";
import { lazy, Suspense } from "react";

const HowItWorksSection = lazy(() => import("../components/landing/HowItWorksSection"));
const BeforeAfterSection = lazy(() => import("../components/landing/BeforeAfterSection"));
const MarketplacesSection = lazy(() => import("../components/landing/MarketplacesSection"));
const PricingSection = lazy(() => import("../components/landing/PricingSection"));
const FAQSection = lazy(() => import("../components/landing/FAQSection"));
const FooterSection = lazy(() => import("../components/landing/FooterSection"));
```

Wrap the lazy sections in a single Suspense boundary with a minimal fallback (empty div or a subtle loading indicator -- NOT a spinner, to avoid layout shift):

```tsx
export default function LandingPage() {
  return (
    <div className="min-h-screen">
      <HeroSection />
      <Suspense fallback={<div className="min-h-screen" />}>
        <HowItWorksSection />
        <BeforeAfterSection />
        <MarketplacesSection />
        <PricingSection />
        <FAQSection />
        <FooterSection />
      </Suspense>
    </div>
  );
}
```

The Suspense fallback uses min-h-screen to prevent layout shift while chunks load. Vite's code splitting will automatically create separate chunks for each lazy import.

Ensure all section components use `export default function SectionName()` (not named exports) for compatibility with React.lazy, which requires default exports.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/marketfoto/frontend && grep -c "og:title" index.html && grep -c "og:description" index.html && grep -c "React.lazy\|lazy(" src/pages/LandingPage.tsx && grep -c "Suspense" src/pages/LandingPage.tsx && echo "PASS: SEO tags present + lazy loading configured"</automated>
  </verify>
  <done>
- index.html has title "MarketFoto — карточки для WB и Ozon за 30 секунд" (per D-08, LAND-08)
- index.html has meta description with value proposition and keyword mentions (per LAND-08)
- index.html has Open Graph tags (og:title, og:description, og:url, og:site_name, og:locale) (per LAND-08)
- index.html has Twitter Card meta tags (per LAND-08)
- LandingPage.tsx uses React.lazy for 6 below-fold sections (per LAND-10)
- HeroSection loads eagerly for fast LCP (per LAND-10)
- Suspense fallback uses min-h-screen to prevent layout shift (per LAND-10)
- Performance preconnect hints for fonts (per LAND-10)
  </done>
</task>

</tasks>

<verification>
1. index.html contains `<title>MarketFoto` tag
2. index.html contains og:title and og:description meta tags
3. index.html contains canonical URL pointing to marketfoto.ru
4. LandingPage.tsx uses React.lazy for at least 5 section imports
5. LandingPage.tsx wraps lazy sections in Suspense
6. HeroSection is NOT lazy-loaded (eager import at top)
</verification>

<success_criteria>
- SEO title matches "MarketFoto -- карточки для WB и Ozon за 30 секунд" (grep confirms)
- OG tags are present: og:title, og:description, og:url, og:site_name, og:locale (grep confirms all 5)
- LandingPage uses React.lazy for below-fold sections (grep confirms "lazy(")
- HeroSection is imported eagerly (grep confirms standard import, not lazy)
- No render-blocking patterns in below-fold content (code-split by Vite)
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-02-SUMMARY.md`
</output>
