---
phase: 10-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/components/landing/HeroSection.tsx
  - frontend/src/components/landing/HowItWorksSection.tsx
  - frontend/src/components/landing/BeforeAfterSection.tsx
  - frontend/src/components/landing/MarketplacesSection.tsx
  - frontend/src/components/landing/PricingSection.tsx
  - frontend/src/components/landing/FAQSection.tsx
  - frontend/src/components/landing/FooterSection.tsx
  - frontend/src/router.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07, LAND-09]

must_haves:
  truths:
    - "Visitor sees hero section with headline, subtitle, CTA button, and before/after image comparison"
    - "Visitor can scroll through How It Works (3 steps), Before/After examples, marketplace logos, pricing, and FAQ"
    - "Pricing section shows 3 plan cards with month/year toggle"
    - "FAQ section has 5-7 collapsible questions with accordion behavior"
    - "Footer contains navigation links and contact info"
    - "Page is fully responsive from 320px mobile to 1280px+ desktop"
  artifacts:
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Landing page assembling all sections"
      exports: ["default"]
    - path: "frontend/src/components/landing/HeroSection.tsx"
      provides: "Hero with headline, CTA, before/after slider"
      exports: ["default"]
    - path: "frontend/src/components/landing/HowItWorksSection.tsx"
      provides: "3-step process section with icons"
      exports: ["default"]
    - path: "frontend/src/components/landing/BeforeAfterSection.tsx"
      provides: "3-4 before/after comparison examples"
      exports: ["default"]
    - path: "frontend/src/components/landing/MarketplacesSection.tsx"
      provides: "WB/Ozon/YM logos section"
      exports: ["default"]
    - path: "frontend/src/components/landing/PricingSection.tsx"
      provides: "3 pricing cards with month/year toggle"
      exports: ["default"]
    - path: "frontend/src/components/landing/FAQSection.tsx"
      provides: "Accordion FAQ with 5-7 questions"
      exports: ["default"]
    - path: "frontend/src/components/landing/FooterSection.tsx"
      provides: "Footer with links and contact"
      exports: ["default"]
    - path: "frontend/src/router.tsx"
      provides: "/ route updated to render LandingPage"
  key_links:
    - from: "frontend/src/pages/LandingPage.tsx"
      to: "frontend/src/components/landing/*.tsx"
      via: "React imports and composition"
      pattern: "import.*from.*landing/"
    - from: "frontend/src/components/landing/HeroSection.tsx"
      to: "/auth?mode=register"
      via: "CTA button link"
      pattern: "auth\\?mode=register"
    - from: "frontend/src/components/landing/PricingSection.tsx"
      to: "/auth?mode=register"
      via: "Plan selection buttons"
      pattern: "auth\\?mode=register"
    - from: "frontend/src/router.tsx"
      to: "frontend/src/pages/LandingPage.tsx"
      via: "Route definition for /"
      pattern: "path.*\"/\".*LandingPage"
---

<objective>
Build the complete marketing landing page with all 7 sections: Hero (headline + before/after slider + CTA), How It Works (3 steps), Before/After examples, Marketplace logos, Pricing cards, FAQ accordion, and Footer. The page is public (no auth required) and mobile-first responsive.

Purpose: This is the conversion page -- visitors land here, understand the value, see proof, and click "Попробовать бесплатно" to register. Every section is designed to reduce friction and demonstrate the product.

Output: 9 new files (1 page + 7 section components + updated router). Landing replaces the placeholder at /.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page/10-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/router.tsx:
```typescript
// "/" route is a placeholder <div> -- replace with <LandingPage />
// "/" is OUTSIDE ProtectedRoute children (no auth required)
// "/auth" route exists for redirect targets
```

From frontend/src/lib/api.ts:
```typescript
export interface User {
  plan: string; credits_remaining: number; // ...
}
```

From frontend/src/components/Header.tsx:
```typescript
// Landing page should NOT use the Header component (that's for authenticated pages)
// Landing has its own header/nav in HeroSection
```

From router.tsx structure:
```typescript
const router = createBrowserRouter([
  { path: "/auth", element: <AuthPage /> },
  { element: <ProtectedRoute />, children: [
    { path: "/editor", ... },
    { path: "/upload", ... },
    { path: "/processing/:imageId", ... },
    { path: "/dashboard", ... },
  ]},
  { path: "/", element: /* REPLACE THIS */ },
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page sections (Hero, HowItWorks, BeforeAfter, Marketplaces)</name>
  <files>frontend/src/components/landing/HeroSection.tsx, frontend/src/components/landing/HowItWorksSection.tsx, frontend/src/components/landing/BeforeAfterSection.tsx, frontend/src/components/landing/MarketplacesSection.tsx</files>
  <action>
Read first:
- frontend/src/pages/UploadPage.tsx (existing component style/TailwindCSS patterns)
- frontend/src/components/Header.tsx (existing design language)
- frontend/src/router.tsx (route structure to understand where / sits)

Create the frontend/src/components/landing/ directory.

**HeroSection.tsx (per D-01, LAND-01):**

Top navigation bar (not the authenticated Header): fixed/sticky top, white bg with shadow-sm. Left: "MarketFoto" logo text (text-xl font-bold). Right: "Войти" link (text-gray-700 hover:text-blue-600) linking to /auth, and "Попробовать бесплатно" button (bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700) linking to /auth?mode=register. Use react-router `Link` component.

Hero content (pt-24 to account for fixed nav, pb-16):
- Container: max-w-6xl mx-auto px-4.
- Layout: on desktop (lg:flex lg:items-center lg:gap-12), text left + image right. On mobile: stacked, text on top.
- Headline: "Профессиональные карточки для WB и Ozon за 30 секунд" (text-4xl lg:text-5xl font-bold text-gray-900 leading-tight). Per D-01.
- Subtitle: "Загрузите фото товара — AI уберёт фон, вы выберете шаблон с инфографикой и скачаете готовую карточку. Без фотографа и дизайнера." (text-lg text-gray-600 mt-4 max-w-lg).
- CTA button: "Попробовать бесплатно" (bg-blue-600 text-white text-lg px-8 py-4 rounded-xl hover:bg-blue-700 shadow-lg mt-6). Link to /auth?mode=register. Below button: small text "3 карточки бесплатно, без карты" (text-sm text-gray-500 mt-2).
- Right side: Before/After comparison. Implement as a CSS-based slider using an `<input type="range">`:
  - Container: relative, aspect-[3/4], max-w-md, rounded-2xl overflow-hidden shadow-2xl.
  - Two overlapping images (before = original phone photo, after = processed card). Since we don't have real images yet, use colored placeholder divs: "before" = bg-gray-300 with text "Фото с телефона" centered, "after" = bg-gradient-to-br from-blue-500 to-purple-600 with text "Готовая карточка" centered and a badge overlay.
  - The "after" image is clipped using `clip-path: inset(0 0 0 {percent}%)` controlled by a range input (0-100). Default at 50%.
  - Range input: absolute bottom-4, w-3/4 mx-auto, styled with accent-blue-600. A vertical divider line at the slider position (absolute, h-full, w-0.5, bg-white, left={percent}%).
  - This approach requires no JS library -- pure CSS + one useState for the slider value.

**HowItWorksSection.tsx (per D-02, LAND-02):**
- Section: py-16 bg-white.
- Container: max-w-6xl mx-auto px-4.
- Title: "Как это работает" (text-3xl font-bold text-center text-gray-900 mb-12).
- 3 steps in grid (grid grid-cols-1 md:grid-cols-3 gap-8):
  - Step 1: Camera icon (inline SVG, 48x48, text-blue-600). Title: "Загрузите фото". Description: "Сфотографируйте товар на телефон и загрузите в сервис. AI уберёт фон за 5 секунд."
  - Step 2: Grid/Template icon (inline SVG, 48x48, text-blue-600). Title: "Выберите шаблон". Description: "Подберите шаблон с инфографикой для вашего маркетплейса. Добавьте текст и бейджи."
  - Step 3: Download icon (inline SVG, 48x48, text-blue-600). Title: "Скачайте карточку". Description: "Готовая карточка в правильном размере для WB, Ozon или Яндекс.Маркет."
- Each step: text-center, with a numbered circle (h-10 w-10 rounded-full bg-blue-100 text-blue-600 font-bold flex items-center justify-center mx-auto mb-4) showing 1, 2, 3.
- Connecting arrows between steps on desktop (hidden on mobile): use a thin line or arrow via CSS (pseudo-element or a separate flex element).

**BeforeAfterSection.tsx (per D-03, LAND-03):**
- Section: py-16 bg-gray-50.
- Title: "До и После" (text-3xl font-bold text-center text-gray-900 mb-12).
- Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6, max-w-6xl mx-auto px-4.
- 4 examples (одежда, электроника, косметика, еда):
  - Each card: rounded-xl overflow-hidden shadow-md bg-white.
  - Top half: "До" placeholder -- bg-gray-200 aspect-square flex items-center justify-center, text "Фото с телефона" + category emoji (e.g., shirt emoji for одежда).
  - Bottom half: "После" placeholder -- gradient background matching marketplace colors, with text "Готовая карточка" + marketplace badge.
  - Category label below: text-sm font-medium text-gray-700 text-center py-3 (e.g., "Одежда", "Электроника", "Косметика", "Еда").
- NOTE: Using placeholder colored divs because real product images don't exist yet. The structure supports swapping in real images later by replacing the placeholder divs with `<img>` tags.

**MarketplacesSection.tsx (per D-04, LAND-04):**
- Section: py-16 bg-white.
- Title: "Для какого маркетплейса" (text-3xl font-bold text-center text-gray-900 mb-4).
- Subtitle: "Правильные размеры автоматически" (text-lg text-gray-500 text-center mb-12). Per D-04.
- 3 marketplace cards in flex (flex flex-col md:flex-row items-center justify-center gap-8):
  - Wildberries: purple gradient card (bg-gradient-to-br from-purple-500 to-purple-700), white text "Wildberries", size info "900 x 1200", rounded-xl p-8 shadow-lg, min-w-[200px].
  - Ozon: blue gradient card (from-blue-500 to-blue-700), white text "Ozon", size "1200 x 1200".
  - Яндекс.Маркет: yellow gradient card (from-yellow-400 to-yellow-600), dark text "Яндекс.Маркет", size "800 x 800".
- No actual logo images needed -- styled text cards with brand colors are sufficient and avoid trademark issues for MVP.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/marketfoto/frontend && ls src/components/landing/HeroSection.tsx src/components/landing/HowItWorksSection.tsx src/components/landing/BeforeAfterSection.tsx src/components/landing/MarketplacesSection.tsx 2>&1 && grep -l "Попробовать бесплатно" src/components/landing/HeroSection.tsx && grep -l "Как это работает" src/components/landing/HowItWorksSection.tsx && grep -l "До и После" src/components/landing/BeforeAfterSection.tsx && grep -l "Для какого маркетплейса" src/components/landing/MarketplacesSection.tsx && echo "PASS: All 4 sections exist with correct headings"</automated>
  </verify>
  <done>
- HeroSection.tsx: fixed nav bar with login/register links, headline "Профессиональные карточки для WB и Ozon за 30 секунд", CTA "Попробовать бесплатно" linking to /auth?mode=register, before/after CSS slider (per D-01, LAND-01)
- HowItWorksSection.tsx: 3 numbered steps (upload, template, download) in responsive grid with icons (per D-02, LAND-02)
- BeforeAfterSection.tsx: 4 before/after example cards (одежда, электроника, косметика, еда) with placeholder images (per D-03, LAND-03)
- MarketplacesSection.tsx: 3 marketplace cards with brand colors and correct dimensions (WB 900x1200, Ozon 1200x1200, YM 800x800) (per D-04, LAND-04)
- All sections are mobile-first responsive (per D-09, LAND-09)
  </done>
</task>

<task type="auto">
  <name>Task 2: Pricing, FAQ, Footer sections + LandingPage assembly + router</name>
  <files>frontend/src/components/landing/PricingSection.tsx, frontend/src/components/landing/FAQSection.tsx, frontend/src/components/landing/FooterSection.tsx, frontend/src/pages/LandingPage.tsx, frontend/src/router.tsx</files>
  <action>
Read first:
- frontend/src/router.tsx (current route config -- replace "/" placeholder)
- frontend/src/components/landing/HeroSection.tsx (verify it exists from Task 1)

**PricingSection.tsx (per D-05, LAND-05):**
- Section: py-16 bg-gray-50, id="pricing" (for anchor links from nav/footer).
- Title: "Тарифы" (text-3xl font-bold text-center text-gray-900 mb-4).
- Month/Year toggle: centered, bg-gray-200 rounded-full p-1 inline-flex. Two buttons: "Месяц" and "Год" with active state (bg-white text-gray-900 shadow-sm) and inactive (text-gray-600). Use useState for billingPeriod ('monthly' | 'annual'). When annual: show "Экономия 33%" badge next to the toggle (text-xs bg-green-100 text-green-700 px-2 py-0.5 rounded-full).
- 3 plan cards in grid (grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto):

  Card 1 - Бесплатный:
  - bg-white rounded-2xl p-6 border border-gray-200.
  - Price: "0 ₽" (text-4xl font-bold). Period: "/мес".
  - Features (list with checkmark icons, text-gray-700): "3 карточки в месяц", "Все шаблоны (базовые)", "Удаление фона AI", "Водяной знак".
  - Button: "Начать бесплатно" (w-full py-3 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50). Link to /auth?mode=register.

  Card 2 - Стартер (POPULAR, visually emphasized):
  - bg-white rounded-2xl p-6 border-2 border-blue-500 relative.
  - "Популярный" badge: absolute -top-3 left-1/2 -translate-x-1/2 bg-blue-600 text-white text-xs font-bold px-3 py-1 rounded-full.
  - Price: monthly = "499 ₽", annual = "333 ₽" (text-4xl font-bold text-blue-600). Period: "/мес". If annual, show strikethrough "499 ₽" in text-sm text-gray-400 line-through.
  - Features: "50 карточек в месяц", "Все шаблоны (включая премиум)", "Удаление фона AI", "Без водяного знака", "Приоритетная обработка".
  - Button: "Выбрать Стартер" (w-full py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700). Link to /auth?mode=register.

  Card 3 - Бизнес:
  - bg-white rounded-2xl p-6 border border-gray-200.
  - Price: monthly = "990 ₽", annual = "659 ₽" (text-4xl font-bold). Period: "/мес".
  - Features: "Безлимит карточек", "Все шаблоны (включая премиум)", "Удаление фона AI", "Без водяного знака", "Приоритетная обработка", "Пакетная загрузка".
  - Button: "Выбрать Бизнес" (w-full py-3 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50). Link to /auth?mode=register.

- Below cards: "Разовая покупка" block (text-center mt-8): "Или купите 1 карточку за 49 ₽ без подписки" (text-gray-600).

**FAQSection.tsx (per D-06, LAND-06):**
- Section: py-16 bg-white, id="faq".
- Title: "Частые вопросы" (text-3xl font-bold text-center text-gray-900 mb-12).
- Container: max-w-3xl mx-auto px-4.
- Accordion: useState<number | null>(null) to track which question is open (null = all closed). Click toggles open/closed. Only one open at a time.
- 7 questions (per D-06):
  1. "Какие форматы фото поддерживаются?" -- "JPG, PNG и WebP. Максимальный размер файла — 10 МБ. Минимальное разрешение — 200x200 пикселей."
  2. "Сколько стоит сервис?" -- "Есть бесплатный тариф на 3 карточки в месяц. Стартер — 499 ₽/мес за 50 карточек. Бизнес — 990 ₽/мес без ограничений. Можно купить одну карточку за 49 ₽."
  3. "Какое качество удаления фона?" -- "Мы используем нейросеть BiRefNet — одну из лучших на рынке. Качество сопоставимо с ручной обработкой в Photoshop для большинства товаров."
  4. "Как быстро готова карточка?" -- "Удаление фона занимает 5-10 секунд. Весь процесс от загрузки фото до скачивания готовой карточки — около 30 секунд."
  5. "Для каких маркетплейсов подходит?" -- "Wildberries (900x1200), Ozon (1200x1200) и Яндекс.Маркет (800x800). Размеры подставляются автоматически."
  6. "Как оплатить?" -- "Банковской картой через ЮKassa. Поддерживаются Visa, MasterCard, МИР. Подписка продлевается автоматически, отменить можно в любой момент."
  7. "Можно ли использовать с API?" -- "API планируется в следующих версиях. Сейчас доступен только веб-интерфейс."

Each FAQ item:
- Border-b border-gray-200 py-4.
- Question: button with w-full text-left flex justify-between items-center. Text: text-lg font-medium text-gray-900. Arrow icon on right (rotates 180deg when open, transition-transform duration-200).
- Answer: conditional render (open === index). Text: text-gray-600 mt-2 pb-2. Animate with max-height transition or simple conditional render.

**FooterSection.tsx (per D-07, LAND-07):**
- Footer: bg-gray-900 text-gray-400 py-12.
- Container: max-w-6xl mx-auto px-4.
- Layout: grid grid-cols-1 md:grid-cols-3 gap-8.
  - Column 1: "MarketFoto" (text-xl font-bold text-white mb-2). Description: "Создавайте профессиональные карточки для маркетплейсов за секунды" (text-sm).
  - Column 2: "Навигация" heading (text-sm font-semibold text-gray-300 uppercase mb-3). Links: "О сервисе" (#hero), "Тарифы" (#pricing), "FAQ" (#faq). Each: text-gray-400 hover:text-white text-sm block mb-2. Use `<a href="#pricing">` for same-page anchors.
  - Column 3: "Поддержка" heading. Links: "support@marketfoto.ru" (mailto:), "Политика конфиденциальности" (link to /privacy or #, placeholder).
- Bottom bar: border-t border-gray-800 mt-8 pt-8. Text: "2026 MarketFoto. Все права защищены." (text-sm text-gray-500 text-center).

**LandingPage.tsx:**
- Simple assembly component importing and rendering all 7 sections in order:
  1. HeroSection
  2. HowItWorksSection
  3. BeforeAfterSection
  4. MarketplacesSection
  5. PricingSection
  6. FAQSection
  7. FooterSection
- Wrapper: min-h-screen. No bg class needed (sections define their own backgrounds).
- Export as default.

**Update router.tsx:**
- Import LandingPage from "./pages/LandingPage".
- Replace the "/" placeholder element with `<LandingPage />`.
- The "/" route must remain OUTSIDE the ProtectedRoute children (landing is public).
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/marketfoto/frontend && ls src/components/landing/PricingSection.tsx src/components/landing/FAQSection.tsx src/components/landing/FooterSection.tsx src/pages/LandingPage.tsx 2>&1 && grep -c "LandingPage" src/router.tsx && grep -l "Тарифы" src/components/landing/PricingSection.tsx && grep -l "Частые вопросы" src/components/landing/FAQSection.tsx && grep -l "MarketFoto" src/components/landing/FooterSection.tsx && echo "PASS: All sections + page + router wired"</automated>
  </verify>
  <done>
- PricingSection.tsx: 3 plan cards (Бесплатный/Стартер/Бизнес) with month/year toggle, correct prices (499/990 monthly, 333/659 annual), feature lists, CTA buttons linking to /auth?mode=register, "Разовая покупка 49 ₽" note (per D-05, LAND-05)
- FAQSection.tsx: 7-question accordion with expand/collapse, covering formats, pricing, quality, speed, marketplaces, payment, API (per D-06, LAND-06)
- FooterSection.tsx: 3-column footer with branding, navigation (anchors to #pricing, #faq), support email, privacy link (per D-07, LAND-07)
- LandingPage.tsx: assembles all 7 sections in order
- router.tsx: "/" route renders LandingPage (public, outside ProtectedRoute)
- All sections are mobile-first responsive (per D-09, LAND-09)
  </done>
</task>

</tasks>

<verification>
1. All 7 section component files exist in frontend/src/components/landing/
2. LandingPage.tsx imports and renders all 7 sections in correct order
3. router.tsx "/" route points to LandingPage (not placeholder)
4. CTA buttons link to /auth?mode=register (grep confirms)
5. Pricing shows correct prices: 0/499/990 monthly (grep confirms)
6. FAQ has 7 questions with accordion behavior
7. Footer has support@marketfoto.ru and copyright text
8. All sections use responsive Tailwind classes (grid-cols-1 + breakpoint variants)
</verification>

<success_criteria>
- Hero headline is "Профессиональные карточки для WB и Ozon за 30 секунд" (grep confirms)
- Pricing toggle switches between monthly/annual prices
- FAQ accordion opens/closes questions (useState tracks open index)
- Footer has anchor links (#pricing, #faq) for same-page navigation
- "/" route renders LandingPage publicly (outside ProtectedRoute)
- Mobile-first: all grids have single-column mobile layout (grid-cols-1)
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-01-SUMMARY.md`
</output>
