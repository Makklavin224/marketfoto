---
phase: 08-payments-credits
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/api/payments.ts
  - frontend/src/pages/PricingPage.tsx
  - frontend/src/pages/PaymentSuccessPage.tsx
  - frontend/src/components/PaymentModal.tsx
  - frontend/src/router.tsx
autonomous: true
requirements:
  - UI-07
  - UI-08
  - UI-09

must_haves:
  truths:
    - "User sees pricing page with 3 plan cards (Free/Starter 499r/Business 990r), month/year toggle, and one-time purchase block"
    - "Clicking a plan's 'Subscribe' button calls POST /api/payments/create and redirects to YooKassa checkout"
    - "After successful payment, user lands on /payment/success page showing green checkmark and new plan"
    - "When credits reach 0, a modal appears with upgrade options (subscription or one-time 49r purchase)"
    - "Annual prices show savings badge (Starter annual 3990r vs 5988r, Business 7900r vs 11880r)"
  artifacts:
    - path: "frontend/src/api/payments.ts"
      provides: "Payment API client functions"
      exports: ["paymentsApi"]
    - path: "frontend/src/pages/PricingPage.tsx"
      provides: "Pricing page with plan cards and toggles"
      min_lines: 100
    - path: "frontend/src/pages/PaymentSuccessPage.tsx"
      provides: "Payment success confirmation page"
      min_lines: 30
    - path: "frontend/src/components/PaymentModal.tsx"
      provides: "Credits exhausted modal with payment options"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/PricingPage.tsx"
      to: "/api/payments/create"
      via: "paymentsApi.create(variant)"
      pattern: "paymentsApi\\.create"
    - from: "frontend/src/pages/PricingPage.tsx"
      to: "YooKassa checkout"
      via: "window.location.href = confirmation_url"
      pattern: "window\\.location\\.href"
    - from: "frontend/src/router.tsx"
      to: "frontend/src/pages/PricingPage.tsx"
      via: "route /pricing"
      pattern: "path.*pricing"
---

<objective>
Build the pricing page, payment success page, and credits-exhausted modal for the MarketFoto frontend.

Purpose: Users need to see plan options, initiate payments, and be prompted to upgrade when credits run out. This is the monetization frontend -- the conversion funnel from free user to paying customer. The pricing page must clearly show value (card count, watermark removal, templates), the success page must confirm the purchase, and the modal must intercept zero-credit users with a compelling upgrade prompt.

Output: 3 new pages/components + API client module + updated router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-payments-credits/08-CONTEXT.md
@.planning/phases/08-payments-credits/08-01-SUMMARY.md

<interfaces>
<!-- Backend API contracts from Plan 01 -->

POST /api/payments/create:
```
Request:  { "variant": "starter" | "business" | "starter_annual" | "business_annual" | "one_time" }
Response: { "payment_id": "uuid", "confirmation_url": "https://yookassa.ru/checkout/..." }
Auth: Bearer token required
```

GET /api/payments/history:
```
Query params: limit=20, offset=0
Response: { "payments": [...], "total": number }
Auth: Bearer token required
```

<!-- Existing frontend contracts -->

From frontend/src/lib/api.ts:
```typescript
// axios instance with JWT interceptor
const api = axios.create({ baseURL: "/api" });
// Attaches Bearer token from localStorage automatically
// Redirects to /auth on 401

export interface User {
  id: string;
  email: string;
  full_name?: string;
  plan: string;           // "free" | "starter" | "business"
  credits_remaining: number;
  subscription_expires_at?: string;
}
```

From frontend/src/stores/auth.ts:
```typescript
export const useAuthStore = create<AuthState>((set) => ({
  user: User | null,
  token: string | null,
  initialize: () => Promise<void>,  // fetches /api/auth/me
  // ...
}));
```

From frontend/src/router.tsx:
```typescript
// Routes: /auth, /editor (protected), /dashboard (protected), /
// Need to add: /pricing (public), /payment/success (protected)
```

From frontend/src/components/ProtectedRoute.tsx:
```typescript
// Wraps protected routes, redirects to /auth if not authenticated
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payment API client and PricingPage with plan cards and toggle</name>
  <files>frontend/src/api/payments.ts, frontend/src/pages/PricingPage.tsx</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/stores/auth.ts, frontend/src/pages/AuthPage.tsx, frontend/src/router.tsx</read_first>
  <action>
Create `frontend/src/api/payments.ts`:
```typescript
import api from "../lib/api";

export type PaymentVariant = "starter" | "business" | "starter_annual" | "business_annual" | "one_time";

export interface CreatePaymentResponse {
  payment_id: string;
  confirmation_url: string;
}

export const paymentsApi = {
  create: (variant: PaymentVariant) =>
    api.post<CreatePaymentResponse>("/payments/create", { variant }),

  history: (limit = 20, offset = 0) =>
    api.get<{ payments: PaymentRecord[]; total: number }>("/payments/history", { params: { limit, offset } }),

  cancelSubscription: () =>
    api.post<{ message: string; plan_active_until: string | null }>("/payments/cancel-subscription"),
};

export interface PaymentRecord {
  id: string;
  type: string;
  plan: string | null;
  amount: number;
  currency: string;
  status: string;
  created_at: string;
}
```

Create `frontend/src/pages/PricingPage.tsx` (per D-09, UI-07):

Build a responsive pricing page with:

1. **Header**: "Тарифы" title, subtitle "Выберите подходящий план"

2. **Billing toggle**: useState for `isAnnual` (boolean). Two buttons: "Ежемесячно" / "Ежегодно" with active state styling. When annual is selected, show savings badge.

3. **Three plan cards** in a responsive grid (1 col mobile, 3 cols desktop via Tailwind `grid grid-cols-1 md:grid-cols-3 gap-6`):

   **Free card:**
   - Price: "0 р/мес"
   - Features: "3 карточки в месяц", "Водяной знак", "Базовые шаблоны"
   - Button: "Текущий план" (disabled) if user.plan === "free", or "Перейти на бесплатный" (no-op)
   - Styling: border-gray-200, simple

   **Starter card:**
   - Monthly: "499 р/мес" | Annual: "3 990 р/год" with crossed-out "5 988 р" and savings "Экономия 33%"
   - Features: "50 карточек в месяц", "Без водяного знака", "Все шаблоны", "Приоритетная обработка"
   - Button: "Подключить Starter" -> calls handleSubscribe("starter") or handleSubscribe("starter_annual")
   - Styling: border-blue-500, ring highlight (recommended plan)

   **Business card:**
   - Monthly: "990 р/мес" | Annual: "7 900 р/год" with crossed-out "11 880 р" and savings "Экономия 34%"
   - Features: "200 карточек в месяц", "Без водяного знака", "Все шаблоны", "Приоритетная обработка", "Пакетная загрузка" (with "скоро" badge)
   - Button: "Подключить Business" -> calls handleSubscribe("business") or handleSubscribe("business_annual")
   - Styling: border-purple-500

4. **One-time purchase block** below the cards (per D-09):
   - "Или купите разово" section
   - "1 карточка — 49 р" with "Купить" button -> calls handleSubscribe("one_time")
   - Subtitle: "Без подписки, без водяного знака"

5. **handleSubscribe function**:
   ```typescript
   const handleSubscribe = async (variant: PaymentVariant) => {
     setLoading(variant);
     try {
       const { data } = await paymentsApi.create(variant);
       window.location.href = data.confirmation_url; // Redirect to YooKassa
     } catch (err) {
       toast.error("Ошибка при создании платежа");
     } finally {
       setLoading(null);
     }
   };
   ```

6. Show loading spinner on the button being clicked. Disable all buttons while a payment is being created.

7. If user already has an active plan, show "Текущий план" on their current plan card with a green checkmark, and disable that button.

Use `react-hot-toast` for error notifications (already in project dependencies).
Use `useAuthStore` to get current user plan for highlighting.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/marketfoto/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - PricingPage renders 3 plan cards with correct prices per D-02/D-09
    - Monthly/annual toggle switches displayed prices and savings badges
    - One-time purchase block shows 49r option
    - handleSubscribe calls POST /api/payments/create and redirects to confirmation_url
    - Current plan is highlighted with "Текущий план" badge
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: PaymentSuccessPage, PaymentModal, and router wiring</name>
  <files>frontend/src/pages/PaymentSuccessPage.tsx, frontend/src/components/PaymentModal.tsx, frontend/src/router.tsx</files>
  <read_first>frontend/src/router.tsx, frontend/src/stores/auth.ts, frontend/src/lib/api.ts, frontend/src/pages/PricingPage.tsx</read_first>
  <action>
Create `frontend/src/pages/PaymentSuccessPage.tsx` (per D-09, UI-08):

1. On mount, call `useAuthStore.getState().initialize()` to refresh user data (plan/credits may have changed via webhook)
2. Display:
   - Large green checkmark icon (SVG or Tailwind-styled div)
   - "Оплата прошла успешно!" heading
   - Show new plan: "Ваш тариф: {plan}" (map plan to Russian: starter -> "Starter", business -> "Business")
   - Show credits: "Доступно карточек: {credits_remaining}"
   - Two buttons:
     - "Создать карточку" -> navigate to /editor
     - "На главную" -> navigate to /dashboard
3. If user data hasn't loaded yet (webhook might be slow), show a loading spinner with "Обрабатываем платеж..." for up to 5 seconds, then show the page with whatever data is available
4. Use `useEffect` with a timer: call `initialize()` immediately, then again after 3 seconds (in case webhook was delayed)

Create `frontend/src/components/PaymentModal.tsx` (per D-09, UI-09):

1. Accepts props: `isOpen: boolean`, `onClose: () => void`
2. Modal overlay with centered card (Tailwind: fixed inset-0 z-50, backdrop blur)
3. Content:
   - Sad/warning icon
   - "Карточки закончились" heading
   - "У вас 0 карточек. Выберите способ продолжить:" subtitle
   - Two options:
     - **One-time purchase card**: "1 карточка — 49 р" with "Купить" button -> calls `paymentsApi.create("one_time")` then `window.location.href = confirmation_url`
     - **Subscription link**: "Или подключите подписку от 499 р/мес" with "Смотреть тарифы" button -> `navigate("/pricing")`, close modal
   - "Закрыть" (X) button to dismiss
4. Export the modal component AND a convenience hook:
   ```typescript
   export function usePaymentModal() {
     const [isOpen, setIsOpen] = useState(false);
     const open = () => setIsOpen(true);
     const close = () => setIsOpen(false);
     return { isOpen, open, close };
   }
   ```
   This hook will be used by editor/render pages (Phase 7) to show the modal when credits_remaining === 0.

Update `frontend/src/router.tsx`:
- Import PricingPage and PaymentSuccessPage
- Add `/pricing` as a PUBLIC route (no ProtectedRoute wrapper -- visitors should see pricing before signing up)
- Add `/payment/success` inside the ProtectedRoute children (must be logged in to see success page)
- Keep all existing routes intact

Final router structure:
```typescript
[
  { path: "/auth", element: <AuthPage /> },
  { path: "/pricing", element: <PricingPage /> },
  {
    element: <ProtectedRoute />,
    children: [
      { path: "/editor", element: ... },
      { path: "/dashboard", element: ... },
      { path: "/payment/success", element: <PaymentSuccessPage /> },
    ],
  },
  { path: "/", element: ... },
]
```
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/marketfoto/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - PaymentSuccessPage shows green checkmark, new plan, credits, and navigation buttons (UI-08)
    - PaymentSuccessPage refreshes user data on mount to pick up webhook updates
    - PaymentModal shows "Карточки закончились" with one-time purchase and subscription options (UI-09)
    - usePaymentModal hook exported for Phase 7 editor/render integration
    - /pricing route is public, /payment/success is protected
    - Router includes all existing + new routes
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. PricingPage imports and renders (check via `grep -r "PricingPage" frontend/src/`)
3. PaymentSuccessPage is routed at /payment/success
4. PaymentModal exports usePaymentModal hook
5. Router has /pricing (public) and /payment/success (protected)
</verification>

<success_criteria>
- Pricing page shows 3 plans with month/year toggle and one-time purchase (UI-07)
- Correct prices displayed: Starter 499r/3990r, Business 990r/7900r, One-time 49r
- Subscribe button redirects to YooKassa via API
- Payment success page refreshes user data and shows confirmation (UI-08)
- Credits-exhausted modal offers one-time purchase or subscription redirect (UI-09)
- All routes properly configured in router
</success_criteria>

<output>
After completion, create `.planning/phases/08-payments-credits/08-03-SUMMARY.md`
</output>
